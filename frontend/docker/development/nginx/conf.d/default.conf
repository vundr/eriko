server {
    listen 80;
    charset utf-8;

    location / {
       root /app/public;
       index index.html;
    }

    location /services {
       proxy_pass http://backend:8080;
       rewrite ^/services(.*) $1 break;
    }
}