version: "3.8"

services:
  frontend:
    build:
      context: frontend/docker/development/nginx
    ports:
      - "80:80"
    volumes:
      - ./frontend:/app
    depends_on:
      - backend

  backend:
    build:
      context: api
    platform: linux/amd64
